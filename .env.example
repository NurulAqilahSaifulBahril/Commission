# Copy to .env and paste your JWT (finance pack + all commission scripts).
# Token only — no "Bearer " prefix. Never commit .env (gitignored).

PG_PROXY_URL=https://pg-proxy-production.up.railway.app/api/sql
PG_PROXY_TOKEN=paste_your_jwt_token_here
PG_PROXY_DB=prod_main
